name: MLOps Pipeline

on: [push]

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: pip install -r requ.txt

      - name: Run pipeline
        run: python main.py
